enum GameState {
  WAIT = "wait",
  PLAY = "play",
}

enum TEXT_ALIGN {
  LEFT = "LEFT",
  RIGHT = "RIGHT",
}

enum GAME_OPTION {
  "ONE" = 1,
  "ZERO" = 0,
  "MINUS_ONE" = -1,
}

export { GameState, GAME_OPTION, TEXT_ALIGN }
